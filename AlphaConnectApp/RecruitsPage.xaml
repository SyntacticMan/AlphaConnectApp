<Page
    x:Class="AlphaConnectApp.RecruitsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlphaConnectApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView x:Name="LVRecruits" HorizontalAlignment="Left" Height="507" VerticalAlignment="Top" Width="380" Margin="10,123,0,0" DataContext="{Binding}" Loaded="LVRecruits_Loaded">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock x:Name="textBlock" Height="100" TextWrapping="Wrap" Text="{Binding name}" Width="380" FontSize="36" FontWeight="Bold"/>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>

    </Grid>
</Page>
