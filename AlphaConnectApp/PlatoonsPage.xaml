<Page
    x:Class="AlphaConnectApp.PlatoonsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlphaConnectApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView x:Name="LVPlatoons" HorizontalAlignment="Left" Height="519" Margin="10,111,0,0" VerticalAlignment="Top" Width="380" DataContext="{Binding}" Loaded="LVPlatoons_Loaded">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock x:Name="textBlock" HorizontalAlignment="Right" Margin="0,0,0,0" TextWrapping="Wrap" Text="{Binding name}" VerticalAlignment="Top" FontSize="36" FontWeight="Normal" TextAlignment="Center"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
